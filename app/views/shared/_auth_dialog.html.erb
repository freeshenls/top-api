<dialog data-dialog-target="dialog" data-dialog-allow-click-outside-value="true" data-action="turbo:submit-end->dialog#submitEnd" class="backdrop:bg-slate-900/50 backdrop:backdrop-blur-sm rounded-[2.5rem] shadow-2xl p-0 w-full max-w-lg border-none bg-white animate-in fade-in zoom-in duration-300 m-auto max-h-[90vh] overflow-y-auto">
  <div class="relative p-8 lg:p-12">
    <!-- Close Button -->
    <button data-action="click->dialog#close" class="absolute top-6 right-6 h-10 w-10 flex items-center justify-center rounded-full bg-slate-50 text-slate-400 hover:bg-slate-100 hover:text-slate-600 transition-all border border-slate-100">
      <%= heroicon "x-mark", variant: :mini, options: { class: "h-5 w-5" } %>
    </button>

    <div class="mb-8 text-center">
      <div class="h-16 w-16 bg-gradient-to-br from-[#0EA5E9] to-[#0369A1] rounded-2xl flex items-center justify-center shadow-lg shadow-sky-200 mx-auto mb-6">
        <%= image_tag asset_path('logo.svg'), class: "h-10 w-10 brightness-0 invert", alt: "Logo" %>
      </div>
      <h2 class="text-2xl font-black text-slate-800 leading-tight">欢迎回到 AI 领航</h2>
      <p class="text-sm text-slate-500 mt-2">实战派 AI 导航，助力您的创作与工作</p>
    </div>

    <%= turbo_frame_tag "auth_frame" do %>
      <div class="flex items-center justify-center py-12">
        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-sky-500"></div>
      </div>
    <% end %>
    
    <div class="mt-8 pt-8 border-t border-slate-100 text-center">
      <p class="text-[11px] text-slate-400 leading-relaxed uppercase tracking-widest font-bold">
        登录即表示您同意我们的 <%= link_to "服务协议", terms_path, class: "text-sky-500 hover:underline", data: { turbo: false } %> 和 <%= link_to "隐私政策", privacy_path, class: "text-sky-500 hover:underline", data: { turbo: false } %>
      </p>
    </div>
  </div>
</dialog>
